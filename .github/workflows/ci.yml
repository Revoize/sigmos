name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          lfs: true
      - name: Set up python
        id: setup-python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          version: "latest"
      - name: Load cached venv
        id: cached-uv-dependencies
        uses: actions/cache@v6
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('./uv.lock') }}
      - name: Install dependencies
        working-directory: .
        if: steps.cached-uv-dependencies.outputs.cache-hit != 'true'
        run: uv sync --all-extras --no-install-project
      - name: Install project
        working-directory: .
        run: |
          uv sync --all-extras
          uv run poe configure
      - name: Run tests
        working-directory: .
        run: uv run poe test
      - name: Run lint
        working-directory: .
        run: uv run poe lint
      - name: Run hooks
        working-directory: .
        run: uv run poe hooks-run

  # Verifies the declared lower bounds actually work: re-resolves direct
  # dependencies to the lowest versions the pyproject constraints allow
  # (transitive deps stay current) and runs the suite across the Python matrix.
  test-lowest:
    name: Test (lowest-direct)
    runs-on: ubuntu-latest
    env:
      UV_RESOLUTION: lowest-direct
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          lfs: true
      - name: Set up python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          version: "latest"
      - name: Install project at lowest-direct versions
        run: |
          uv sync --all-extras
          uv run poe configure
      - name: Run tests
        run: uv run poe test

  # Auto-merge Dependabot's github-actions bumps once CI is green.
  dependabot:
    name: "Dependabot"
    needs: [test, test-lowest]
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'dependabot[bot]' && github.event_name == 'pull_request'}}
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v3
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
      - name: Enable auto-merge for Dependabot PRs
        run: gh pr merge --auto --squash "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
