version: 2
updates:
  # GitHub Actions pins are updated here; Python/uv dependencies are handled by
  # the refresh-lockfile workflow.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
